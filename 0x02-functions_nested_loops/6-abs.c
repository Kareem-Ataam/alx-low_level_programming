#include <stdlib.h>
#include "main.h"
/**
* _abs - Get the absolute value of a give number
* @n: The number to get the absolue to
* Return: The absolute value of the number
*/
int _abs(int n)
{
	return (abs(n));
}
